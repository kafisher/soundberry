ul
    each comment in comments
        li: a.comment(data-comment_id=comment.id)
            img.mini-avatar(src=comment.user.avatar_url)
            strong= comment.user.username
            |  - #{ comment.body }

